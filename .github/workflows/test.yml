name: Tests
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, unlabeled]
    branches:
      - main
  push:
    branches:
      - main

jobs:
  tests:
    name: Run Python Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # Lets the codecov upload authenticate via GitHub OIDC (no token).
      id-token: write
    strategy:
      fail-fast: False
      matrix:
        python-version:
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"

    steps:

    - uses: actions/checkout@v7

    - name: Setup Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install "rawji @ git+https://github.com/pinpox/rawji@5549fdb93028549c2bab8abe963f1a91c50b5368"
        pip install -e . --no-deps
        pip install pytest pytest-cov pytest-github-actions-annotate-failures

    - name: Test with pytest
      run: python -m pytest -m "not gui" --cov --cov-report=term-missing --cov-report=xml

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v7
      with:
        fail_ci_if_error: true
        use_oidc: true
        slug: p5k369/grawji
